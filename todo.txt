https://git-scm.com/docs

/*a körfolyamat helyesen*/
git commit -a -m "b" // ha pull, nem kell
git fetch
git merge
git mergetool // ha nincs conflict nem kell
git commit -a -m "ab"
git push // ha pull, nem kell
